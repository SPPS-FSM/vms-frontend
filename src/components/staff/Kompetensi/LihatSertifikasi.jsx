import { Button, Option, Select } from "@material-tailwind/react";
import React from "react";

export default function LihatSertifikasi({ url }) {
  return <iframe src={url} className="w-full h-full"></iframe>;
}
